[package]
name = "harnless-exec-subprocess"
description = "Subprocess execution-world provider for harnless: owns spawn coordinates and cancellation over std::process, with bounded output capture."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
harnless-seams = { path = "../harnless-seams" }
